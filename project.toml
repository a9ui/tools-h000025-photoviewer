[project]
id = "H000025"
name = "PhotoViewer"
kind = "H"

[commands]
setup = "pnpm install --frozen-lockfile"
dev = "pnpm dev"
verify = "powershell -ExecutionPolicy Bypass -File .\\scripts\\verify-project.ps1"

[links]
github = ""
chatgpt_project = ""
deployment = ""
production = ""

[workflow]
linear = false
source_of_truth = "github"
local_ledger = "sqlite"
