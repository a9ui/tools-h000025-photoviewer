# Plan

1. 実project root、runtime、state/cache hashを固定する。
2. Browserの主要journeyをDOM/Playwrightとcurrent-run screenshotで監査する。
3. Browserのfailure honesty、Delete、catalog ownership、sidebar layoutを修正する。
4. WPFのscan supersessionと20,000枚UI responsivenessをfault injectionで修正する。
5. Browser/WPFの全local gateを順番に実行する。
6. Browser仕様、WPF仕様、findings、quality score、SQLiteを同期する。
